spring:
  docker:
    compose:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    clean-disabled: false

logging:
  level:
    org.hibernate.SQL: warn
    org.sopt.sopkathon: info
